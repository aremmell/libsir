version: ~> 1.0
language: c
branches:
  only:
  - master
dist: bionic
script:
  - make -j tests
  - build/sirtests

jobs:
  include:
    - os: linux
      arch: amd64
      compiler: gcc
    - os: linux
      arch: amd64
      compiler: clang
    - os: linux
      arch: arm64
      compiler: gcc
    - os: linux
      arch: arm64
      compiler: clang
    - os: osx
      osx_image: xcode14.2
      compiler: clang
